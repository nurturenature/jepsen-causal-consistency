name: 'better-sqlite Strong Matrix'

on:
  workflow_dispatch:
  schedule:
    - cron:  '0 0 * * *'

jobs:
  better-sqlite-strong:
    uses: nurturenature/jepsen-causal-consistency/.github/workflows/run-jepsen-tests.yml@main
    with:
      workloads: >
        [ 'better-sqlite-strong' ]
      nemeses: >
        [ 'none' ]
      rates: >
        [ '5',  '10', '15' ]
      time-limits: >
        ['60', '180', '600' ]
